<%= render 'shared/page_header', title: 'Categories', total: number_to_currency(@grand_total) %>

<ul class='category_list'>
  <% @categories.each do |category| %>
    <%= render 'category_card', category: category %>
  <% end %>
</ul>
